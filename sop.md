# Live Knowledge - 产品需求文档 (SOP)

## 🧭 概述

**Live Knowledge** 是一款智能实时知识提取与增强系统。  
它通过持续监控屏幕内容变化，识别潜在的知识点、任务、会议信息等，将其转化为结构化的标签 (Tags)，并通过 AI 引擎实时生成洞察或行动建议。

系统的核心目标是让用户在任何场景下都能 **自动获得上下文知识辅助与任务联动能力**，实现「知识即现」。

---

## 🧩 系统组成

### 1. Screen Watcher（屏幕监听器）

- 实时监控屏幕内容变化（可选区域或全局）。
- 采用 **感知哈希 (Perceptual Hash)** 算法进行差异检测，仅在内容显著变化时触发。
- **捕获窗口 (Capture Window)** 机制：检测到变化后，连续捕获一段时间（如 6 秒）的帧，以理解动态操作流。

**触发条件示例：**

- 用户完成一条聊天信息发送。
- 用户停止输入超过 2 秒。
- 页面结构（如卡片、列表）发生视觉变更。
- 当前窗口焦点切换。

---

### 2. Content Analyzer（内容解析器）

- **多模态分析**：将截取的屏幕图像 + OCR 文本同时发送给 AI 引擎。
- **混合模式 (Hybrid Mode)**：
  - **AI 在线**：使用 Gemini 1.5 Pro / GPT-4o 进行深度语义理解。
  - **本地回退**：使用 Tesseract.js (OCR) + 正则表达式进行离线提取。
- **提取内容**：
  - `meeting_schedule` (会议日程)
  - `task_todo` (待办任务)
  - `code_error` (代码错误/堆栈)
  - `data_insight` (数据/报表分析)

---

### 3. AI Engine（智能知识引擎）

- 接收来自解析器的结构化数据。
- **插件增强**：结合插件提供的上下文（如 Git 分支、日历状态）构建 Prompt。
- **动态响应**：
  - 💬 **洞察 (Insight)**：生成总结或建议。
  - ✅ **行动 (Action)**：生成可执行的操作（如“创建 Linear 任务”）。
- **去重策略**：通过语义哈希和时间窗口（Context TTL）防止重复打扰。

---

### 4. Presentation Layer（知识呈现层）

- **非侵入式设计**：
  - 🪟 **悬浮面板 (Overlay)**：在屏幕边缘或相关内容附近显示。
  - 💡 **智能气泡**：轻量级提示。
  - 🧭 **仪表盘 (Dashboard)**：查看历史记录和统计数据。
- **交互能力**：用户可点击“执行”来触发插件定义的外部动作。

---

### 5. Plugin System（插件系统）

- **上下文注入**：插件可读取外部状态（IDE、浏览器、系统日历）并注入 AI 上下文。
- **动作处理**：插件可注册处理特定类型的 Action（如调用 REST API）。
- **生命周期管理**：支持热插拔、沙箱运行（规划中）和权限控制。

---

### 6. Local Knowledge Store（本地知识库）

- 使用 **SQLite** 本地存储所有数据，保障隐私。
- **实体关联**：建立 知识条目 -> 标签 -> 洞察 的关联关系。
- **语义搜索**：支持对历史捕获内容的全文检索。

---

## 🔧 技术实现现状

| 模块               | 技术栈                                           |
| :----------------- | :----------------------------------------------- |
| **Screen Watcher** | Electron `desktopCapturer` + Perceptual Hash     |
| **OCR & Vision**   | Tesseract.js (Local) + OpenAI/Gemini API (Cloud) |
| **AI Engine**      | Node.js + OpenAI SDK / Google Generative AI SDK  |
| **Backend**        | Electron Main Process (Node.js) + Express Server |
| **Frontend**       | React 19 + TailwindCSS 4 + Zustand               |
| **Storage**        | SQLite (via `sqlite3`)                           |

---

## 🚀 潜在拓展方向

1.  **多模态深度融合**：结合音频输入（会议录音）与屏幕内容进行联合分析。
2.  **主动问答模式**：用户可直接对屏幕内容提问：“这个错误怎么修？”。
3.  **团队协同**：在隐私允许的前提下，共享团队内的知识上下文（如“Alice 昨天也遇到了这个问题”）。
4.  **跨设备同步**：通过端到端加密云服务同步桌面端与移动端的知识。

---

## 📈 关键指标 (KPI)

| 指标                 | 目标值                           |
| :------------------- | :------------------------------- |
| **误触发率**         | < 5%                             |
| **响应延迟**         | < 3s (AI 模式) / < 1s (本地模式) |
| **上下文识别准确率** | > 90%                            |
| **用户采纳率**       | > 30% (对推荐行动的点击率)       |

---

## 🧠 总结

Live Knowledge 是一种「实时知识觉察系统」。它利用视觉感知与 AI 决策，将屏幕上的非结构化像素流实时转化为结构化的知识与行动。核心价值在于 **让知识“主动出现”而非“被搜索”**，并始终坚持 **隐私优先、本地为主** 的设计原则。
